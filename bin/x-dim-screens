#!/bin/sh


active-monitors () {
	xrandr --listactivemonitors | rg '\d: (\+\*|\+)(\S+)' -o -r '$2'
}

if [ -z $1 ];
then
	echo "USAGES: $0 <brightness 0.0 <-> 1.0>"
else

	for monitor in $(active-monitors);
	do
		xrandr --output $monitor --brightness $1
	done
fi

